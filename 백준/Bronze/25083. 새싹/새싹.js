const inputData = [`         ,r'"7`, "r`-_   ,'  ,/", ` \\. ". L_r'`, '   `~\\/', '      |', '      |'];

let solution = (inputData) => {
    for (let i = 0; i < inputData.length; i++) {
        console.log(inputData[i]);
    }
};

solution(inputData);